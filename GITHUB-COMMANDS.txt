# .NET Updater — GitHub Remote Commands
# IMPORTANT: Paste ONE line only. Do not paste script output back into cmd.
# In ScreenConnect, prefer the #!ps blocks (run as PowerShell, not cmd).
# Run installs as admin/SYSTEM. Dry run does not require elevation.
# Allow 5-10 minutes for update runs.

# =============================================================================
# SCREENCONNECT #!ps — DRY RUN (paste ONE line; main script URL only)
# ScreenConnect can truncate longer URLs and break dotnet-updater-screenconnect.ps1
# =============================================================================

#!ps #maxlength=200000 #timeout=300000 $env:DOTNET_UPDATER_DRYRUN = '1' [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12 New-Item -Path "C:\temp" -ItemType Directory -Force -ErrorAction SilentlyContinue | Out-Null $t = Get-Date -Format 'yyyyMMddHHmmss' (New-Object Net.WebClient).DownloadFile("https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater.ps1?nocache=$t", "C:\temp\dotnet-updater.ps1") & "C:\temp\dotnet-updater.ps1" -DryRun

# =============================================================================
# SCREENCONNECT #!ps — UPDATE (paste ONE line; main script, not wrapper)
# =============================================================================

#!ps #maxlength=200000 #timeout=600000 Remove-Item Env:DOTNET_UPDATER_DRYRUN -ErrorAction SilentlyContinue [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12 New-Item -Path "C:\temp" -ItemType Directory -Force -ErrorAction SilentlyContinue | Out-Null $t = Get-Date -Format 'yyyyMMddHHmmss' (New-Object Net.WebClient).DownloadFile("https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater.ps1?nocache=$t", "C:\temp\dotnet-updater.ps1") & "C:\temp\dotnet-updater.ps1"

# =============================================================================
# SCREENCONNECT #!ps — UPDATE via wrapper (optional; log at C:\temp\dotnet-updater-last-run.log)
# Use only if your ScreenConnect field does not truncate the longer URL
# =============================================================================

#!ps #maxlength=200000 #timeout=600000 Remove-Item Env:DOTNET_UPDATER_DRYRUN -ErrorAction SilentlyContinue [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12 New-Item -Path "C:\temp" -ItemType Directory -Force -ErrorAction SilentlyContinue | Out-Null $t = Get-Date -Format 'yyyyMMddHHmmss' (New-Object Net.WebClient).DownloadFile("https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater-screenconnect.ps1?nocache=$t", "C:\temp\dotnet-updater-sc.ps1") & "C:\temp\dotnet-updater-sc.ps1"

# =============================================================================
# CMD ONE-LINER — DRY RUN (calls main script directly)
# =============================================================================

powershell -NoProfile -ExecutionPolicy Bypass -Command "$env:DOTNET_UPDATER_DRYRUN='1';$ErrorActionPreference='Continue';$ProgressPreference='SilentlyContinue';[Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12;$t=Get-Date -Format 'yyyyMMddHHmmss';New-Item -Path C:\temp -ItemType Directory -Force -ErrorAction SilentlyContinue|Out-Null;try{(New-Object Net.WebClient).DownloadFile('https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater.ps1?nocache='+$t,'C:\temp\dotnet-updater.ps1');& C:\temp\dotnet-updater.ps1 -DryRun;exit $LASTEXITCODE}catch{Write-Host 'ERROR:' $_.Exception.Message -ForegroundColor Red;exit 1}"

# =============================================================================
# CMD ONE-LINER — UPDATE (compact output via wrapper)
# =============================================================================

powershell -NoProfile -ExecutionPolicy Bypass -Command "Remove-Item Env:DOTNET_UPDATER_DRYRUN -ErrorAction SilentlyContinue;$ErrorActionPreference='Continue';$ProgressPreference='SilentlyContinue';[Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12;$t=Get-Date -Format 'yyyyMMddHHmmss';New-Item -Path C:\temp -ItemType Directory -Force -ErrorAction SilentlyContinue|Out-Null;try{(New-Object Net.WebClient).DownloadFile('https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater-screenconnect.ps1?nocache='+$t,'C:\temp\dotnet-updater-sc.ps1');& C:\temp\dotnet-updater-sc.ps1;exit $LASTEXITCODE}catch{Write-Host 'ERROR:' $_.Exception.Message -ForegroundColor Red;exit 1}"

# =============================================================================
# CMD ONE-LINER — UPDATE (full output)
# =============================================================================

powershell -NoProfile -ExecutionPolicy Bypass -Command "[Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; New-Item C:\temp -ItemType Directory -Force | Out-Null; $t=Get-Date -Format 'yyyyMMddHHmmss'; (New-Object Net.WebClient).DownloadFile('https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater.ps1?nocache='+$t,'C:\temp\dotnet-updater.ps1'); & C:\temp\dotnet-updater.ps1"

# =============================================================================
# CMD ONE-LINER — UPDATE + REMOVE UNUSED OLD VERSIONS
# =============================================================================

powershell -NoProfile -ExecutionPolicy Bypass -Command "[Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; New-Item C:\temp -ItemType Directory -Force | Out-Null; $t=Get-Date -Format 'yyyyMMddHHmmss'; (New-Object Net.WebClient).DownloadFile('https://raw.githubusercontent.com/monobrau/dotnet-updater/main/dotnet-updater.ps1?nocache='+$t,'C:\temp\dotnet-updater.ps1'); & C:\temp\dotnet-updater.ps1 -RemoveOldVersions"
